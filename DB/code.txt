CREATE TABLE DEPARTMENT(ID INT PRIMARY KEY,DEPT CHAR(50),EMP_ID INT);   
INSERT INTO DEPARTMENT (ID, DEPT, EMP_ID) VALUES (22, "MECH", 789);

cd location.
sqlite3 mydb.db
.databases
.tables
.schema tablename
select * from tablename
	where condition;